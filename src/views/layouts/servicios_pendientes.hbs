<head>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div style="" class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
        <li class="nav-item">
            <a style="color: blue;" class=" nav-link" href="/servicios_pendientes"> <center><i class="fas fa-home fa-2x"></i></center>Inicio</a> 
        </li>
        <li class="nav-item">
          <a class="nav-link"  href="/clientes/"><center><i class="fas fa-address-book fa-2x"></i></center>Clientes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/agregar_registro"><center><i class="fas fa-file-alt fa-2x"></i></center>Registrar <br> <center>Servicio</center> </a>
        </li>
        <li class="nav-item">
          <a  class="nav-link "  href="/reportes"><center><i class="fas fa-paste fa-2x"></i></center> Reportes</a>
        </li>
        
      </ul>
    </div>
  </div>
</nav>
</head>
<div class="container mt-5">
  <div class="row">
    <div class="col-md-20 mx-auto">
      <strong>
      <table class="table table-bordered table-hover" style="color: white;">
        <thead>
          <tr style="background-color: #00aff0; color: white;">
            <th>N°</th>
            <th>Cliente</th>
            <th>Calle</th>
            <th>Clonia</th>
            <th>Fecha</th>
            <th>Hora</th>
            <th>Opciones</th>
          </tr>
        </thead>
        <tbody>
          {{#each clienteg}}
          <tr id="G{{IdOrden}}" style="background-color: #c4c4c4; color:#940008;">
            <td>{{IdOrden}}</td>
            <td>{{Nombre}}</td>
            <td>{{Calle}}</td>
            <td>{{Colonia}}</td>
            <td>{{moment FechaVisita}}</td>
            <td>{{Hora}}</td>
            <td><center><a href="/ver_cliente/{{IdCliente}}" title="VER CLIENTE" class="mx-auto btn btn-info"><i style="color: white;" class="fas fa-user"></i></a></center>
            </td>
          </tr>
          {{/each}}
          {{#each cliente}}
          <tr id="{{IdOrden}}" style="background-color: #c4c4c4; color:black;">
            <td>{{IdOrden}}</td>
            <td style="">{{Nombre}}</td>
            <td>{{Calle}}</td>
            <td>{{Colonia}}</td>
            <td>{{moment FechaVisita}}</td>
            <td>{{Hora}}</td>
            <td><center><a  href="/ver_cliente/{{IdCliente}}" title="VER CLIENTE" class="mx-auto btn btn-info"><i style="color: white;" class="fas fa-user"></i></a></center>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table></strong>
    </div>
  </div>
</div>


<script>

  let array = []
  let arrayD = []
  {{#each pendientes}}
  array.push({
    IdOrdenServicio: "{{IdOrdenServicio}}",
    IdCliente: "{{IdCliente}}",
    FechaVisita: "{{moment FechaVisita}}",
    IdEquipo: "{{IdEquipo}}",
    Falla: "{{Falla}}",
    Hora: "{{Hora}}",
  })
  {{/each}}
  {{#each array}}
  arrayD.push({
    ahora:"{{ahora}}"
  })
  {{/each}}


      ordenes()

      function ordenes() {

        var hoy = new Date();
        let mes = (hoy.getMonth() + 1)
        if (mes < 10) {
          mes = "0" + mes
        }
        let dia = (hoy.getDate())
        if (dia < 10) {
          dia = "0" + dia
        }
        var fecha = hoy.getFullYear() + '-' + mes + '-' + dia

        for (index = 0; index < array.length; index++) {

          if (fecha == array[index].FechaVisita) {
            document.getElementById(array[index].IdOrdenServicio).style.color="blue"
            console.log(arrayD[index].ahora)
            if(arrayD[index].ahora=="si"){
              document.getElementById(array[index].IdOrdenServicio).style.color = "white"

            }
          }
        }

        for(index = 0; index < array.length; index++){

        }

      }



      function actualizar(){location.reload(true);}

setInterval("actualizar()",300000);

</script>